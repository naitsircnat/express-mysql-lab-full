{{#extends "base"}}
  {{#block "content"}}

    <h1>Edit Customer</h1>
    <form method="POST">
      <div class="mb-3">
        <label>First Name</label>
        <input type="text" name="first_name" value="{{customer.first_name}}" class="form-control"/>
      </div>

      <div class="mb-3">
        <label>Last Name</label>
        <input type="text" name="last_name" value="{{customer.last_name}}" class="form-control"/>
      </div>

      <div class="mb-3">
        <label>Rating</label>
        <input type="text" name="rating" value="{{customer.rating}}" class="form-control"/>
      </div>

      <div class="mb-3">
        <label>Company</label>
        <select name="company_id" class="form-control">

          {{#each companies}}

            <option value="{{company_id}}" {{#eq company_id ../customer.company_id}}selected
            {{/eq}}>
            {{name}}</option>

          {{/each}}

        </select>
      </div>

      <button type="submit" class="btn btn-primary">Update Customer</button>
    </form>

  {{/block}}
{{/extends}}